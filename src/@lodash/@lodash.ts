import __ from 'lodash';


const _ = __.runInContext();

_.mixin({
	// Immutable Set for setting state
    // getRandKeyProperty: (obj: object) => {
    //     const arr = _.toArray(obj);

    // }
});

export default _;
